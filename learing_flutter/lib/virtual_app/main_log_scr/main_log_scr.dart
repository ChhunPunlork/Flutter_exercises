import 'package:flutter/material.dart';

class MainLogScr extends StatelessWidget {
  const MainLogScr({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Lork screen"),
      ),
    );
  }
}
